# Metrics Field Requirements

<!-- GENERATED from av-field-dictionary.yaml by scripts/dictionary-ids.py. Do not hand-edit. -->

One requirement per declared field, generated from the HAL Field Dictionary. Each is independently testable, and its identifier is derived from the field path, so it is stable for as long as the field is.

A product must satisfy a requirement only for the fields it declares in `hfp-metrics.yaml`. A field it does not declare is absent at runtime rather than served as zero.

| Requirement | Field | Contract | Definition |
|---|---|---|---|
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.FRAMES_DECODED** | `av.video_decoder.frames_decoded` | `frames` · `counter` · read-only<br>id `0x63c81c7efbe7e743` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Compressed frames the decoder has decoded and emitted at its output (post-decode, pre-sink). +1 per emitted frame. Counted from instance creation; never reset on flush/seek. |
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.FRAMES_DROPPED** | `av.video_decoder.frames_dropped` | `frames` · `counter` · read-only<br>id `0x2c67d98cc76e6fb7` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Frames the decoder discarded **before output** — undecodable (corruption / unsupported) or shed under decode overload. **Excludes** presentation-side drops, which are `av.video_sink`'s. A frame here is not also in `frames_decoded`. |
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.DECODE_ERRORS** | `av.video_decoder.decode_errors` | `events` · `counter` · read-only<br>id `0x070bb5fa11ef497b` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Decode-error occurrences (bitstream/NAL parse failure, codec fault). Decoder-internal. A **distinct axis** from `frames_dropped`: an error need not drop a frame, and a drop need not be an error. |
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.DECODE_LATENCY_SUM_US** | `av.video_decoder.decode_latency_sum_us` | `us` · `counter` · read-only<br>id `0x4e69bac967d540b7` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Running sum of per-frame decode latency = *(decoder emits frame at output)* − *(compressed access unit queued to decoder input)*. Needs the decoder's internal input/output timestamps. Mean = `decode_latency_sum_us / frames_decoded`. |
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.LAST_DECODE_ERROR_PTS_MS** | `av.video_decoder.last_decode_error_pts_ms` | `ms` · `current` · read-only<br>id `0x14debdd0bbd63a14` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Stream PTS at detection of the **most recent** decode error, within ±1 frame interval. Read with `decode_errors`: the counter says how many, this says where the newest one was. Left undeclared where the SoC cannot derive a PTS — never served as `-1`. |
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.LAST_DECODE_ERROR_REASON** | `av.video_decoder.last_decode_error_reason` | `none` · `current` · read-only<br>id `0x4fda2a3cf3d7dc09` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Closed-vocabulary classification of the most recent decode error. This is the value a consumer branches on, and it is what makes the fault comparable across SoCs. |
| **HAL.METRICS.FIELD.AV.VIDEO_DECODER.LAST_DECODE_ERROR_VENDOR_CODE** | `av.video_decoder.last_decode_error_vendor_code` | `none` · `current` · read-only<br>id `0x26d18e085c02dfd4` | Shall be reported as a live sample re-read each poll, absolute, and never summed. The SoC's own error code for the same fault, carried through uninterpreted. For vendor diagnosis; never branched on. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FRAMES_RECEIVED** | `av.video_sink.frames_received` | `frames` · `counter` · read-only<br>id `0x524c81ef37d81082` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Decoded frames received to be presented from the decoder. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FRAMES_PRESENTED** | `av.video_sink.frames_presented` | `frames` · `counter` · read-only<br>id `0x7f36b75533d5db3a` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Frames handed to display scanout for presentation — one per successful render. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FRAMES_DROPPED_LATE** | `av.video_sink.frames_dropped_late` | `frames` · `counter` · read-only<br>id `0x7682bfb371ecc4d5` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Frames dropped because their PTS deadline (vs the render clock) had passed by **> one display frame-interval** at the sink render check — too late to show. Sink render-decision internal. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FRAMES_DROPPED_FRC** | `av.video_sink.frames_dropped_frc` | `frames` · `counter` · read-only<br>id `0x6c43fb5584823d18` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Frames dropped by **frame-rate conversion** when source rate > display rate (cadence decimation) — *not* because they were late. FRC-internal. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FRAMES_REPEATED_FRC** | `av.video_sink.frames_repeated_frc` | `frames` · `counter` · read-only<br>id `0x72b9db4bfce6f2ab` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Frames repeated (held > 1 display interval) by **FRC** when source rate < display rate (cadence up-conversion). Normal content cadence — **not** a freeze. FRC-internal. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FRAMES_REPEATED_MISSING_FRAME** | `av.video_sink.frames_repeated_missing_frame` | `frames` · `counter` · read-only<br>id `0x5a652c2dcf72deac` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Frames repeated **to cover a missing next frame** — the sink had no new decoded frame ready at render time (starvation cover). Feeds `freeze_duration_ms`. Sink-internal. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.UNDERFLOWED** | `av.video_sink.underflowed` | `episodes` · `counter` · read-only<br>id `0x6804c05ae6533181` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Count of video buffer-underflow **episodes**. +1 at each episode **start** (no frame to present at render time); ends when a new frame is presented. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.UNDERFLOW_DURATION_MS** | `av.video_sink.underflow_duration_ms` | `ms` · `counter` · read-only<br>id `0x23e55a88f5bbbd94` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Cumulative wall-clock time across all video-underflow episodes (Σ of each episode's start→clear). Paired with `underflowed`. An underflow episode manifests on screen as a freeze. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FREEZE_DURATION_MS** | `av.video_sink.freeze_duration_ms` | `ms` · `counter` · read-only<br>id `0x1c86c5dfa4db5108` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Derived from `av.video_sink.frames_repeated_missing_frame`, which the product must also declare. Cumulative on-screen time the same frame was held due to **repeated-to-cover-missing-frame** (customer-visible freeze) — the time integral of `frames_repeated_missing_frame`. Excludes FRC cadence repeats. Derived from a Driver figure. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FREEZE_EVENT_COUNT** | `av.video_sink.freeze_event_count` | `events` · `counter` · read-only<br>id `0x5196f6826e91a801` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Derived from `av.video_sink.frames_repeated_missing_frame`, which the product must also declare. Count of freeze episodes — +1 per episode start. Paired with `freeze_duration_ms`: mean freeze length = `freeze_duration_ms` / `freeze_event_count`. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.MAX_FREEZE_DURATION_MS** | `av.video_sink.max_freeze_duration_ms` | `ms` · `high_water` · read-only<br>id `0x088d619c00c1c539` | Shall be reported as a monotone maximum since source creation, absolute. Derived from `av.video_sink.frames_repeated_missing_frame`, which the product must also declare. Longest single freeze episode observed since instance creation. Monotone non-decreasing high-water mark. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.RENDER_LATENCY_SUM_US** | `av.video_sink.render_latency_sum_us` | `us` · `counter` · read-only<br>id `0x75f862796424b00d` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Running sum of per-frame render latency = *(frame presented to scanout)* − *(frame received at sink input)*. Needs the sink's internal present timestamp. Paired with `frames_presented`. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.BUFFER_DEPTH_MS** | `av.video_sink.buffer_depth_ms` | `ms` · `current` · read-only<br>id `0x111d82c20cb9e693` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Buffered decoded media ahead of the present position **now** — Σ presentation-durations of frames queued but not yet presented. Live sample re-read each poll; **not** cumulative. **Warning:** SoC- and use-case-specific. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.LAST_UNDERFLOW_TRIGGER** | `av.video_sink.last_underflow_trigger` | `none` · `current` · read-only<br>id `0x03210d8159685756` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Closed-vocabulary cause of the most recent underflow episode: startup prefill, mid-stream, seek recovery, trickplay recovery, content boundary. Distinguishes an expected starvation from a defect. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.LAST_UNDERFLOW_DURATION_MS** | `av.video_sink.last_underflow_duration_ms` | `ms` · `current` · read-only<br>id `0x7b1ed4a908e8ef1b` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Length of the most recent **completed** underflow episode. Read with `underflowed` and `underflow_duration_ms`, which give the count and the total. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.LAST_DROPPED_FRAME_PTS_MS** | `av.video_sink.last_dropped_frame_pts_ms` | `ms` · `current` · read-only<br>id `0x0e6d081eed2918dd` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Stream PTS of the most recently dropped frame, within ±1 frame interval, whatever the drop axis. Turns a drop count into a locatable defect. Left undeclared where no PTS is derivable. |
| **HAL.METRICS.FIELD.AV.VIDEO_SINK.FIRST_FRAME_PRESENTED_PTS_MS** | `av.video_sink.first_frame_presented_pts_ms` | `ms` · `current` · read-only<br>id `0x56eaeb10709bcdd8` | Shall be reported as a live sample re-read each poll, absolute, and never summed. PTS of the first frame to reach presentation — set at session start and re-set after each seek or flush. With `av.session.preroll_ms` this splits time-to-first-frame into the part middleware owns and the part the hardware owns. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.FRAMES_DECODED** | `av.audio_decoder.frames_decoded` | `frames` · `counter` · read-only<br>id `0x23c94a86f7865005` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Audio access units decoded and emitted at the decoder output. +1 per emitted access unit. Counted from instance creation; never reset on flush/seek. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.FRAMES_DROPPED** | `av.audio_decoder.frames_dropped` | `frames` · `counter` · read-only<br>id `0x748c7fd20b15139d` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Audio frames discarded before output (xruns / undecodable). Decoder-internal. Excludes `frames_decoded`. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.DECODE_ERRORS** | `av.audio_decoder.decode_errors` | `events` · `counter` · read-only<br>id `0x577de621243f5a94` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Audio decode-error occurrences — a **distinct axis** from `frames_dropped`, exactly as on `av.video_decoder`: an error need not drop a frame, and a drop need not be an error. Decoder-internal. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.DECODE_LATENCY_SUM_US** | `av.audio_decoder.decode_latency_sum_us` | `us` · `counter` · read-only<br>id `0x1b3e9a9b42d47872` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Σ per-frame audio decode latency *(output-emitted − input-queued)*. Needs decoder-internal timestamps. Paired with `frames_decoded`. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.LAST_DECODE_ERROR_PTS_MS** | `av.audio_decoder.last_decode_error_pts_ms` | `ms` · `current` · read-only<br>id `0x5d4a1b8bfa10cbbe` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Stream PTS at detection of the most recent audio decode error, within ±1 frame interval. Read with `decode_errors`. Left undeclared where no PTS is derivable — never served as `-1`. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.LAST_DECODE_ERROR_REASON** | `av.audio_decoder.last_decode_error_reason` | `none` · `current` · read-only<br>id `0x10596fb80be50427` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Closed-vocabulary classification of the most recent audio decode error. The value a consumer branches on. |
| **HAL.METRICS.FIELD.AV.AUDIO_DECODER.LAST_DECODE_ERROR_VENDOR_CODE** | `av.audio_decoder.last_decode_error_vendor_code` | `none` · `current` · read-only<br>id `0x7129e447242c26d5` | Shall be reported as a live sample re-read each poll, absolute, and never summed. The SoC's own code for the same fault, carried through uninterpreted. For vendor diagnosis; never branched on. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.UNDERFLOWED** | `av.audio_sink.underflowed` | `episodes` · `counter` · read-only<br>id `0x142f899619c8c4a9` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Count of audio buffer-underflow **episodes**. +1 at each episode start (no decoded audio to present); ends when output resumes. Counterpart of `av.video_sink.underflowed` — without it an audio underflow rate cannot be computed at all. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.UNDERFLOW_DURATION_MS** | `av.audio_sink.underflow_duration_ms` | `ms` · `counter` · read-only<br>id `0x4ce68288bc2f93b2` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Cumulative time the audio path was starved of decoded data (Σ of each episode start→clear). Paired with `underflowed`: mean episode length = `underflow_duration_ms` / `underflowed`. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.SILENCE_DURATION_MS** | `av.audio_sink.silence_duration_ms` | `ms` · `counter` · read-only<br>id `0x09fdcdf6715ef048` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Cumulative emitted **digital silence** at or above the HFP-declared threshold (default 500 ms minimum episode). Digital-silence detection is in the audio path. Distinguishes unexpected absence of audio output from starvation (`underflow_duration_ms`). |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.SILENCE_EVENT_COUNT** | `av.audio_sink.silence_event_count` | `events` · `counter` · read-only<br>id `0x357a57be2601412e` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Count of completed digital-silence periods at or above the HFP-declared threshold. Paired with `silence_duration_ms`: mean silence length = `silence_duration_ms` / `silence_event_count`. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.BUFFER_DEPTH_MS** | `av.audio_sink.buffer_depth_ms` | `ms` · `current` · read-only<br>id `0x1aea4fc8a27262b7` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Buffered decoded audio ahead of the play position **now**. Live sample re-read each poll; **not** cumulative. **Warning:** SoC- and use-case-specific. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.LAST_UNDERFLOW_TRIGGER** | `av.audio_sink.last_underflow_trigger` | `none` · `current` · read-only<br>id `0x40771a4a2b2ee087` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Closed-vocabulary cause of the most recent audio underflow episode, same vocabulary as `av.video_sink.last_underflow_trigger`. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.LAST_UNDERFLOW_DURATION_MS** | `av.audio_sink.last_underflow_duration_ms` | `ms` · `current` · read-only<br>id `0x638f410fc657c525` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Length of the most recent **completed** audio underflow episode. Read with `underflowed` and `underflow_duration_ms`, which give the count and the total. |
| **HAL.METRICS.FIELD.AV.AUDIO_SINK.LAST_SILENCE_DURATION_MS** | `av.audio_sink.last_silence_duration_ms` | `ms` · `current` · read-only<br>id `0x1cf88c9352312e83` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Length of the most recent completed digital-silence period. Read with `silence_event_count` and `silence_duration_ms`. |
| **HAL.METRICS.FIELD.AV.CLOCK.SYNC_OFFSET_MS** | `av.clock.sync_offset_ms` | `ms` · `current` · read-only<br>id `0x0853e5868c5f2bd8` | Shall be reported as a live sample re-read each poll, absolute, and never summed. The driver's predicted current audio-vs-video presentation offset. **Sign:** audio leads → **positive**, video leads → **negative**. Live sample, not cumulative. |
| **HAL.METRICS.FIELD.AV.CLOCK.SYNC_MAX_ABS_OFFSET_MS** | `av.clock.sync_max_abs_offset_ms` | `ms` · `high_water` · read-only<br>id `0x20d08f573aeb1229` | Shall be reported as a monotone maximum since source creation, absolute. Derived from `av.clock.sync_offset_ms`, which the product must also declare. Largest `abs(sync_offset_ms)` observed since instance creation. Monotone non-decreasing high-water mark — derived from `sync_offset_ms`. |
| **HAL.METRICS.FIELD.AV.CLOCK.SYNC_TIME_OVER_THRESHOLD_MS** | `av.clock.sync_time_over_threshold_ms` | `ms` · `counter` · read-only<br>id `0x323c3766c9e3638c` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Derived from `av.clock.sync_threshold_ms`, which the product must also declare. Cumulative time `abs(sync_offset_ms)` exceeded `sync_threshold_ms` — the time integral of out-of-sync operation. Derived from `sync_offset_ms` and `sync_threshold_ms`. |
| **HAL.METRICS.FIELD.AV.CLOCK.SYNC_THRESHOLD_MS** | `av.clock.sync_threshold_ms` | `ms` · `config` · read-write<br>id `0x4c981a6fd5bbf977` | Shall be reported as the present value of a tunable, absolute. The offset magnitude beyond which playback counts as out of sync. The threshold `sync_time_over_threshold_ms` integrates against and `sync_max_abs_offset_ms` is judged by — declaring those two without this leaves both uninterpretable. Writable so an integrator can tighten it per product. |
| **HAL.METRICS.FIELD.AV.CLOCK.RESYNC_COUNT** | `av.clock.resync_count` | `events` · `counter` · read-only<br>id `0x1e11b09c212e1a59` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Count of A/V-sync **corrections** the clock applied — each discrete realignment to re-align audio and video. +1 per applied correction. |
| **HAL.METRICS.FIELD.AV.CLOCK.RESYNC_MAGNITUDE_SUM_MS** | `av.clock.resync_magnitude_sum_ms` | `ms` · `counter` · read-only<br>id `0x7be51ba179aa2379` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Σ of the **absolute magnitude** (ms) of each resync correction. Clock-internal. Paired with `resync_count`: mean correction size = `resync_magnitude_sum_ms / resync_count`. |
| **HAL.METRICS.FIELD.AV.SESSION.TIME_PLAYING_MS** | `av.session.time_playing_ms` | `ms` · `counter` · read-only<br>id `0x070f48f400dfd832` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Cumulative wall-clock time the session spent in `PLAYING`. The denominator every rate and ratio is computed against — without it a consumer cannot turn a drop count into a drop rate. Accumulated by the session state machine. |
| **HAL.METRICS.FIELD.AV.SESSION.TIME_BUFFERING_MS** | `av.session.time_buffering_ms` | `ms` · `counter` · read-only<br>id `0x23bb1bcb7c5998c5` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Cumulative time the session was stalled awaiting data, as the state machine observed it — distinct from `av.video_sink.underflow_duration_ms`, which is the sink's view of starvation. |
| **HAL.METRICS.FIELD.AV.SESSION.PREROLL_MS** | `av.session.preroll_ms` | `ms` · `current` · read-only<br>id `0x39fc344ee2e672e7` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Time from `play()` to the pipeline reaching `PLAYING`, per playback start. With the driver-sourced `av.video_sink.first_frame_presented_pts_ms` this splits time-to-first-frame into the part middleware owns and the part the hardware owns. |
| **HAL.METRICS.FIELD.AV.SESSION.SEEK_COUNT** | `av.session.seek_count` | `events` · `counter` · read-only<br>id `0x1e61cf035b838d2e` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Completed seek/flush cycles. The denominator for post-seek recovery figures, and the context that explains why first-frame timing was re-set. |
| **HAL.METRICS.FIELD.AV.ADMISSION.DENIED_COUNT** | `av.admission.denied_count` | `events` · `counter` · read-only<br>id `0x2f3b167773559817` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Session-creation requests refused as unsatisfiable. Lets a consumer distinguish **"playback never started"** from **"playback started and then failed"** — two very different customer experiences, indistinguishable from playback counters alone. |
| **HAL.METRICS.FIELD.AV.ADMISSION.RECLAIM_COUNT** | `av.admission.reclaim_count` | `events` · `counter` · read-only<br>id `0x27a466d1c15111b0` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Times a session's resources were reclaimed for another admitted consumer. Explains an otherwise inexplicable teardown. |
| **HAL.METRICS.FIELD.HEALTH.POLL.POLL_PERIOD_MS** | `health.poll.poll_period_ms` | `ms` · `current` · read-only<br>id `0x6983c999eb4a2057` | Shall be reported as a live sample re-read each poll, absolute, and never summed. The poll loop's **actual** most-recent period, against the declared cadence floor. A declared cadence is an intention; this is the measurement. |
| **HAL.METRICS.FIELD.HEALTH.POLL.POLL_OVERRUN_COUNT** | `health.poll.poll_overrun_count` | `events` · `counter` · read-only<br>id `0x306beef00e98e4dc` | Shall be reported as cumulative since source creation, monotonically non-decreasing, and never reset on flush or seek. Poll cycles that exceeded the declared floor. Direct evidence for or against the ≤50 ms freshness contract. |
| **HAL.METRICS.FIELD.HEALTH.POLL.STATE_AGE_MS** | `health.poll.state_age_ms` | `ms` · `current` · read-only<br>id `0x71a67dc5c92a91bd` | Shall be reported as a live sample re-read each poll, absolute, and never summed. Age of the canonical state at the moment this read was served — how stale the numbers in this snapshot are. Lets a consumer reason about freshness instead of assuming it. |
