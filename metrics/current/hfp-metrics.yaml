#** *****************************************************************************
# *
# * If not stated otherwise in this file or this component's LICENSE file the
# * following copyright and licenses apply:
# *
# * Copyright 2026 RDK Management
# *
# * Licensed under the Apache License, Version 2.0 (the "License");
# * you may not use this file except in compliance with the License.
# * You may obtain a copy of the License at
# *
# * http://www.apache.org/licenses/LICENSE-2.0
# *
# * Unless required by applicable law or agreed to in writing, software
# * distributed under the License is distributed on an "AS IS" BASIS,
# * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# * See the License for the specific language governing permissions and
# * limitations under the License.
# *
#** ******************************************************************************

# HAL Feature Profile for metrics
#
# RDK Metrics Declaration — format "metrics/0.1".
# VENDOR file: what this SoC measures. Validated by hfp-metrics-schema.yaml.
#
# NAMING — every metric name is four segments, fully qualified, no short form:
#
#     <domain>.<element>.<instance>.<field>    av.video_decoder.0.frames_decoded
#
#   The first three address a source (getSource("av.video_decoder.0")); the
#   fourth selects a field within it.
#
#   THIS FILE DECLARES THE ELEMENT, NOT THE INSTANCE. It states that a
#   video_decoder reports frames_decoded, once, plus `instances: 2` — it never
#   enumerates .0 and .1. The live set comes from getSourcePaths() at runtime,
#   because it is dynamic: a picture-in-picture decoder exists only while the
#   second session does.
#
# ALIGNS WITH
#   HPK HFP convention        file + schema pair, pykwalify-validated
#   OpenTelemetry semantic    dotted names against a central registry of
#     conventions / Prometheus name + unit + type; instance as a dimension
#                             rather than baked into the name
#   ethtool -S string tables  the schema travels with the interface; a consumer
#                             reads what exists and ignores what it does not know
#   com.rdk.hal.metrics       the AIDL this declares for; getCapabilities()
#                             returns the union of every declaration on the device
#
# RULES THAT BITE
#   * A name absent from this file is ABSENT at runtime — never served as 0.
#     "This SoC cannot measure it" and "it measured zero" must stay distinct.
#   * DECLARE THE WHOLE DICTIONARY, COMMENT OUT WHAT YOU CANNOT SERVE, one line
#     each, with "# NOT SUPPORTED — <reason>" on the same line. The file then
#     reads as a checklist against the dictionary: a reviewer scans one column
#     and sees what this SoC does not do, rather than having to notice
#     something missing.
#   * Every name must be defined in the domain dictionary at the declared
#     dictionaryVersion. There is no SoC-private namespace: a figure only one
#     SoC can produce still gets a dictionary entry, so no consumer ever grows
#     per-SoC code.
#   * DESCRIPTIONS AND IDS ARE GENERATED, NEVER HAND-EDITED. Both are written by
#         metrics/current/scripts/dictionary-ids.py --sync
#     from docs/av_field_dictionary.md at the pinned dictionaryVersion, which
#     remains the source of truth. Edit the dictionary and re-run --sync.
#     Deleting an id regenerates it, so a rename or a unit change flows through.
#   * AN ID IS THE FIELD'S CONTRACT, NOT ITS INDEX. It is the first 8 bytes of
#     SHA-256 over "<domain>.<element>.<field>|<unit>|<kind>", so nothing
#     allocates it and there is no registry to consult. A consumer compares it
#     against the id it was built with: a product declaring a name it serves in
#     the wrong unit, or with the wrong kind, is a hard mismatch rather than a
#     silently wrong number.
#   * pollCadenceMs may be TIGHTER than the 50 ms floor, never looser.
#   * `instances` is the count the hardware SUPPORTS, not the count that is
#     live. It makes capability checkable before boot, lets a test assert that
#     no source index >= instances ever appears, and MUST agree with the owning
#     HAL's own feature profile — a CI-catchable cross-check.
#
# Every value is int64 (AIDL long, signed 64-bit).

metrics:
  interfaceVersion: "0.1.0.0"     # frozen com.rdk.hal.metrics version
  schemaVersion: "0.1.0"          # this file's schema revision

  domains:
    - domain: av
      dictionaryVersion: "1.1"    # revision of the av field dictionary

      elements:

        # Sources    av.video_decoder.0 (main), av.video_decoder.1 (PiP)
        # Read       getSource("av.video_decoder.0")->getAll()
        # Aggregate  sum frames_dropped over sources matching av.video_decoder.*
        - element: video_decoder
          instances: 2            # dual decode: main + PiP. Must match
                                  # hfp-videodecoder.yaml. Live sources are a
                                  # subset: av.video_decoder.0 .. .1
          pollCadenceMs: 20       # tighter than the 50 ms floor; never looser
          fields:

            # frames_decoded: Compressed frames the decoder has decoded and emitted at its
            #                 output (post-decode, pre-sink). +1 per emitted frame. Counted from
            #                 instance creation; never reset on flush/seek.

            # frames_decoded: Compressed frames the decoder has decoded and emitted at its
            #                 output (post-decode, pre-sink). +1 per emitted frame. Counted from
            #                 instance creation; never reset on flush/seek.

            # frames_decoded: Compressed frames the decoder has decoded and emitted at its
            #                 output (post-decode, pre-sink). +1 per emitted frame. Counted from
            #                 instance creation; never reset on flush/seek.
            - { name: frames_decoded, unit: frames, kind: counter, id: 0x63c81c7efbe7e743 }

            # frames_dropped: Frames the decoder discarded before output — undecodable
            #                 (corruption / unsupported) or shed under decode overload. Excludes
            #                 presentation-side drops (Section 3.2). A frame here is not also in
            #                 frames_decoded.

            # frames_dropped: Frames the decoder discarded before output — undecodable
            #                 (corruption / unsupported) or shed under decode overload. Excludes
            #                 presentation-side drops, which are av.video_sink's. A frame here
            #                 is not also in frames_decoded.

            # frames_dropped: Frames the decoder discarded before output — undecodable
            #                 (corruption / unsupported) or shed under decode overload. Excludes
            #                 presentation-side drops, which are av.video_sink's. A frame here
            #                 is not also in frames_decoded.
            - { name: frames_dropped, unit: frames, kind: counter, id: 0x2c67d98cc76e6fb7 }

            # decode_errors: Decode-error occurrences (bitstream/NAL parse failure, codec
            #                fault). Decoder-internal. A distinct axis from frames_dropped: an
            #                error need not drop a frame, and a drop need not be an error.

            # decode_errors: Decode-error occurrences (bitstream/NAL parse failure, codec
            #                fault). Decoder-internal. A distinct axis from frames_dropped: an
            #                error need not drop a frame, and a drop need not be an error.

            # decode_errors: Decode-error occurrences (bitstream/NAL parse failure, codec
            #                fault). Decoder-internal. A distinct axis from frames_dropped: an
            #                error need not drop a frame, and a drop need not be an error.
            - { name: decode_errors, unit: events, kind: counter, id: 0x070bb5fa11ef497b }

            # decode_latency_sum_us: Running sum of per-frame decode latency = (decoder emits
            #                        frame at output) − (compressed access unit queued to
            #                        decoder input). Needs the decoder's internal input/output
            #                        timestamps. Mean = decode_latency_sum_us / frames_decoded.

            # decode_latency_sum_us: Running sum of per-frame decode latency = (decoder emits
            #                        frame at output) − (compressed access unit queued to
            #                        decoder input). Needs the decoder's internal input/output
            #                        timestamps. Mean = decode_latency_sum_us / frames_decoded.

            # decode_latency_sum_us: Running sum of per-frame decode latency = (decoder emits
            #                        frame at output) − (compressed access unit queued to
            #                        decoder input). Needs the decoder's internal input/output
            #                        timestamps. Mean = decode_latency_sum_us / frames_decoded.
            - { name: decode_latency_sum_us, unit: us, kind: counter, id: 0x4e69bac967d540b7 }

            # last_decode_error_pts_ms: Stream PTS at detection of the most recent decode error,
            #                           within ±1 frame interval. Read with decode_errors: the
            #                           counter says how many, this says where the newest one
            #                           was. Left undeclared where the SoC cannot derive a PTS —
            #                           never served as -1.

            # last_decode_error_pts_ms: Stream PTS at detection of the most recent decode error,
            #                           within ±1 frame interval. Read with decode_errors: the
            #                           counter says how many, this says where the newest one
            #                           was. Left undeclared where the SoC cannot derive a PTS —
            #                           never served as -1.

            # last_decode_error_pts_ms: Stream PTS at detection of the most recent decode error,
            #                           within ±1 frame interval. Read with decode_errors: the
            #                           counter says how many, this says where the newest one
            #                           was. Left undeclared where the SoC cannot derive a PTS —
            #                           never served as -1.
            - { name: last_decode_error_pts_ms, unit: ms, kind: current, id: 0x14debdd0bbd63a14 }

            # last_decode_error_reason: Closed-vocabulary classification of the most recent
            #                           decode error. This is the value a consumer branches on,
            #                           and it is what makes the fault comparable across SoCs.

            # last_decode_error_reason: Closed-vocabulary classification of the most recent
            #                           decode error. This is the value a consumer branches on,
            #                           and it is what makes the fault comparable across SoCs.

            # last_decode_error_reason: Closed-vocabulary classification of the most recent
            #                           decode error. This is the value a consumer branches on,
            #                           and it is what makes the fault comparable across SoCs.
            - { name: last_decode_error_reason, unit: none, kind: current, id: 0x4fda2a3cf3d7dc09 }

            # last_decode_error_vendor_code: The SoC's own error code for the same fault,
            #                                carried through uninterpreted. For vendor
            #                                diagnosis; never branched on.

            # last_decode_error_vendor_code: The SoC's own error code for the same fault,
            #                                carried through uninterpreted. For vendor
            #                                diagnosis; never branched on.

            # last_decode_error_vendor_code: The SoC's own error code for the same fault,
            #                                carried through uninterpreted. For vendor
            #                                diagnosis; never branched on.
            - { name: last_decode_error_vendor_code, unit: none, kind: current, id: 0x26d18e085c02dfd4 }

        # Sources    av.video_sink.0, av.video_sink.1
        # Episodes   underflowed counts them, underflow_duration_ms totals them,
        #            last_underflow_duration_ms describes the most recent one
        - element: video_sink
          instances: 2            # one per decode path
          fields:

            # frames_received: Decoded frames received to be presented from the decoder.

            # frames_received: Decoded frames received to be presented from the decoder.

            # frames_received: Decoded frames received to be presented from the decoder.
            - { name: frames_received, unit: frames, kind: counter, id: 0x524c81ef37d81082 }

            # frames_presented: Frames handed to display scanout for presentation — one per
            #                   successful render.

            # frames_presented: Frames handed to display scanout for presentation — one per
            #                   successful render.

            # frames_presented: Frames handed to display scanout for presentation — one per
            #                   successful render.
            - { name: frames_presented, unit: frames, kind: counter, id: 0x7f36b75533d5db3a }

            # frames_dropped_late: Frames dropped because their PTS deadline (vs the render
            #                      clock) had passed by > one display frame-interval at the sink
            #                      render check — too late to show. Sink render-decision
            #                      internal.

            # frames_dropped_late: Frames dropped because their PTS deadline (vs the render
            #                      clock) had passed by > one display frame-interval at the sink
            #                      render check — too late to show. Sink render-decision
            #                      internal.

            # frames_dropped_late: Frames dropped because their PTS deadline (vs the render
            #                      clock) had passed by > one display frame-interval at the sink
            #                      render check — too late to show. Sink render-decision
            #                      internal.
            - { name: frames_dropped_late, unit: frames, kind: counter, id: 0x7682bfb371ecc4d5 }

            # frames_dropped_frc: Frames dropped by frame-rate conversion when source rate >
            #                     display rate (cadence decimation) — not because they were
            #                     late. FRC-internal.

            # frames_dropped_frc: Frames dropped by frame-rate conversion when source rate >
            #                     display rate (cadence decimation) — not because they were
            #                     late. FRC-internal.

            # frames_dropped_frc: Frames dropped by frame-rate conversion when source rate >
            #                     display rate (cadence decimation) — not because they were
            #                     late. FRC-internal.
            - { name: frames_dropped_frc, unit: frames, kind: counter, id: 0x6c43fb5584823d18 }

            # frames_repeated_frc: Frames repeated (held > 1 display interval) by FRC when
            #                      source rate < display rate (cadence up-conversion). Normal
            #                      content cadence — not a freeze. FRC-internal.

            # frames_repeated_frc: Frames repeated (held > 1 display interval) by FRC when
            #                      source rate < display rate (cadence up-conversion). Normal
            #                      content cadence — not a freeze. FRC-internal.

            # frames_repeated_frc: Frames repeated (held > 1 display interval) by FRC when
            #                      source rate < display rate (cadence up-conversion). Normal
            #                      content cadence — not a freeze. FRC-internal.
            - { name: frames_repeated_frc, unit: frames, kind: counter, id: 0x72b9db4bfce6f2ab }

            # frames_repeated_missing_frame: Frames repeated to cover a missing next frame — the
            #                                sink had no new decoded frame ready at render time
            #                                (starvation cover). Feeds freeze_duration_ms.
            #                                Sink-internal.

            # frames_repeated_missing_frame: Frames repeated to cover a missing next frame — the
            #                                sink had no new decoded frame ready at render time
            #                                (starvation cover). Feeds freeze_duration_ms.
            #                                Sink-internal.

            # frames_repeated_missing_frame: Frames repeated to cover a missing next frame — the
            #                                sink had no new decoded frame ready at render time
            #                                (starvation cover). Feeds freeze_duration_ms.
            #                                Sink-internal.
            - { name: frames_repeated_missing_frame, unit: frames, kind: counter, id: 0x5a652c2dcf72deac }

            # underflowed: Count of video buffer-underflow episodes. +1 at each episode start
            #              (no frame to present at render time); ends when a new frame is
            #              presented.

            # underflowed: Count of video buffer-underflow episodes. +1 at each episode start
            #              (no frame to present at render time); ends when a new frame is
            #              presented.

            # underflowed: Count of video buffer-underflow episodes. +1 at each episode start
            #              (no frame to present at render time); ends when a new frame is
            #              presented.
            - { name: underflowed, unit: episodes, kind: counter, id: 0x6804c05ae6533181 }

            # underflow_duration_ms: Cumulative wall-clock time across all video-underflow
            #                        episodes (sum of of each episode's start->clear). Paired
            #                        with underflowed. An underflow episode manifests on screen
            #                        as a freeze.

            # underflow_duration_ms: Cumulative wall-clock time across all video-underflow
            #                        episodes (sum of of each episode's start->clear). Paired
            #                        with underflowed. An underflow episode manifests on screen
            #                        as a freeze.

            # underflow_duration_ms: Cumulative wall-clock time across all video-underflow
            #                        episodes (sum of of each episode's start->clear). Paired
            #                        with underflowed. An underflow episode manifests on screen
            #                        as a freeze.
            - { name: underflow_duration_ms, unit: ms, kind: counter, id: 0x23e55a88f5bbbd94 }

            # freeze_duration_ms: Cumulative on-screen time the same frame was held due to
            #                     repeated-to-cover-missing-frame (customer-visible freeze) —
            #                     the time integral of frames_repeated_missing_frame. Excludes
            #                     FRC cadence repeats. Derived from a Driver figure.

            # freeze_duration_ms: Cumulative on-screen time the same frame was held due to
            #                     repeated-to-cover-missing-frame (customer-visible freeze) —
            #                     the time integral of frames_repeated_missing_frame. Excludes
            #                     FRC cadence repeats. Derived from a Driver figure.

            # freeze_duration_ms: Cumulative on-screen time the same frame was held due to
            #                     repeated-to-cover-missing-frame (customer-visible freeze) —
            #                     the time integral of frames_repeated_missing_frame. Excludes
            #                     FRC cadence repeats. Derived from a Driver figure.
            - { name: freeze_duration_ms, unit: ms, kind: counter, id: 0x1c86c5dfa4db5108 }

            # freeze_event_count: Count of freeze episodes — +1 per episode start. Paired with
            #                     freeze_duration_ms: mean freeze length = freeze_duration_ms /
            #                     freeze_event_count.

            # freeze_event_count: Count of freeze episodes — +1 per episode start. Paired with
            #                     freeze_duration_ms: mean freeze length = freeze_duration_ms /
            #                     freeze_event_count.

            # freeze_event_count: Count of freeze episodes — +1 per episode start. Paired with
            #                     freeze_duration_ms: mean freeze length = freeze_duration_ms /
            #                     freeze_event_count.
            - { name: freeze_event_count, unit: events, kind: counter, id: 0x5196f6826e91a801 }

            # max_freeze_duration_ms: Longest single freeze episode observed since instance
            #                         creation. Monotone non-decreasing high-water mark.

            # max_freeze_duration_ms: Longest single freeze episode observed since instance
            #                         creation. Monotone non-decreasing high-water mark.

            # max_freeze_duration_ms: Longest single freeze episode observed since instance
            #                         creation. Monotone non-decreasing high-water mark.
            - { name: max_freeze_duration_ms, unit: ms, kind: high_water, id: 0x088d619c00c1c539 }

            # render_latency_sum_us: Running sum of per-frame render latency = (frame presented
            #                        to scanout) − (frame received at sink input). Needs the
            #                        sink's internal present timestamp. Paired with
            #                        frames_presented.

            # render_latency_sum_us: Running sum of per-frame render latency = (frame presented
            #                        to scanout) − (frame received at sink input). Needs the
            #                        sink's internal present timestamp. Paired with
            #                        frames_presented.

            # render_latency_sum_us: Running sum of per-frame render latency = (frame presented
            #                        to scanout) − (frame received at sink input). Needs the
            #                        sink's internal present timestamp. Paired with
            #                        frames_presented.
            - { name: render_latency_sum_us, unit: us, kind: counter, id: 0x75f862796424b00d }

            # buffer_depth_ms: Buffered decoded media ahead of the present position now — sum of
            #                  presentation-durations of frames queued but not yet presented.
            #                  Live sample re-read each poll; not cumulative. Warning: SoC- and
            #                  use-case-specific.

            # buffer_depth_ms: Buffered decoded media ahead of the present position now — sum of
            #                  presentation-durations of frames queued but not yet presented.
            #                  Live sample re-read each poll; not cumulative. Warning: SoC- and
            #                  use-case-specific.

            # buffer_depth_ms: Buffered decoded media ahead of the present position now — sum of
            #                  presentation-durations of frames queued but not yet presented.
            #                  Live sample re-read each poll; not cumulative. Warning: SoC- and
            #                  use-case-specific.
            - { name: buffer_depth_ms, unit: ms, kind: current, id: 0x111d82c20cb9e693 }

            # last_underflow_trigger: Closed-vocabulary cause of the most recent underflow
            #                         episode: startup prefill, mid-stream, seek recovery,
            #                         trickplay recovery, content boundary. Distinguishes an
            #                         expected starvation from a defect.

            # last_underflow_trigger: Closed-vocabulary cause of the most recent underflow
            #                         episode: startup prefill, mid-stream, seek recovery,
            #                         trickplay recovery, content boundary. Distinguishes an
            #                         expected starvation from a defect.

            # last_underflow_trigger: Closed-vocabulary cause of the most recent underflow
            #                         episode: startup prefill, mid-stream, seek recovery,
            #                         trickplay recovery, content boundary. Distinguishes an
            #                         expected starvation from a defect.
            - { name: last_underflow_trigger, unit: none, kind: current, id: 0x03210d8159685756 }

            # last_underflow_duration_ms: Length of the most recent completed underflow episode.
            #                             Read with underflowed and underflow_duration_ms, which
            #                             give the count and the total.

            # last_underflow_duration_ms: Length of the most recent completed underflow episode.
            #                             Read with underflowed and underflow_duration_ms, which
            #                             give the count and the total.

            # last_underflow_duration_ms: Length of the most recent completed underflow episode.
            #                             Read with underflowed and underflow_duration_ms, which
            #                             give the count and the total.
            - { name: last_underflow_duration_ms, unit: ms, kind: current, id: 0x7b1ed4a908e8ef1b }

            # last_dropped_frame_pts_ms: Stream PTS of the most recently dropped frame, within
            #                            ±1 frame interval, whatever the drop axis. Turns a drop
            #                            count into a locatable defect. Left undeclared where no
            #                            PTS is derivable.

            # last_dropped_frame_pts_ms: Stream PTS of the most recently dropped frame, within
            #                            ±1 frame interval, whatever the drop axis. Turns a drop
            #                            count into a locatable defect. Left undeclared where no
            #                            PTS is derivable.

            # last_dropped_frame_pts_ms: Stream PTS of the most recently dropped frame, within
            #                            ±1 frame interval, whatever the drop axis. Turns a drop
            #                            count into a locatable defect. Left undeclared where no
            #                            PTS is derivable.
            - { name: last_dropped_frame_pts_ms, unit: ms, kind: current, id: 0x0e6d081eed2918dd }

            # first_frame_presented_pts_ms: PTS of the first frame to reach presentation — set
            #                               at session start and re-set after each seek or
            #                               flush. With av.session.preroll_ms this splits
            #                               time-to-first-frame into the part middleware owns
            #                               and the part the hardware owns.

            # first_frame_presented_pts_ms: PTS of the first frame to reach presentation — set
            #                               at session start and re-set after each seek or
            #                               flush. With av.session.preroll_ms this splits
            #                               time-to-first-frame into the part middleware owns
            #                               and the part the hardware owns.

            # first_frame_presented_pts_ms: PTS of the first frame to reach presentation — set
            #                               at session start and re-set after each seek or
            #                               flush. With av.session.preroll_ms this splits
            #                               time-to-first-frame into the part middleware owns
            #                               and the part the hardware owns.
            - { name: first_frame_presented_pts_ms, unit: ms, kind: current, id: 0x56eaeb10709bcdd8 }

        # Sources    av.audio_decoder.0, av.audio_decoder.1
        # Names      av.audio_decoder.frames_decoded - same leaf as the video
        #            decoder; different element, same figure, different media
        - element: audio_decoder
          instances: 2            # main + PiP audio, even where only one is audible
          fields:

            # frames_decoded: Audio access units decoded and emitted at the decoder output. +1
            #                 per emitted access unit. Counted from instance creation; never
            #                 reset on flush/seek.

            # frames_decoded: Audio access units decoded and emitted at the decoder output. +1
            #                 per emitted access unit. Counted from instance creation; never
            #                 reset on flush/seek.

            # frames_decoded: Audio access units decoded and emitted at the decoder output. +1
            #                 per emitted access unit. Counted from instance creation; never
            #                 reset on flush/seek.
            - { name: frames_decoded, unit: frames, kind: counter, id: 0x23c94a86f7865005 }

            # frames_dropped: Audio frames discarded before output (xruns / undecodable).
            #                 Decoder-internal. Excludes frames_decoded.

            # frames_dropped: Audio frames discarded before output (xruns / undecodable).
            #                 Decoder-internal. Excludes frames_decoded.

            # frames_dropped: Audio frames discarded before output (xruns / undecodable).
            #                 Decoder-internal. Excludes frames_decoded.
            - { name: frames_dropped, unit: frames, kind: counter, id: 0x748c7fd20b15139d }

            # decode_errors: Audio decode-error occurrences — a distinct axis from
            #                frames_dropped, exactly as on av.video_decoder: an error need not
            #                drop a frame, and a drop need not be an error. Decoder-internal.

            # decode_errors: Audio decode-error occurrences — a distinct axis from
            #                frames_dropped, exactly as on av.video_decoder: an error need not
            #                drop a frame, and a drop need not be an error. Decoder-internal.

            # decode_errors: Audio decode-error occurrences — a distinct axis from
            #                frames_dropped, exactly as on av.video_decoder: an error need not
            #                drop a frame, and a drop need not be an error. Decoder-internal.
            - { name: decode_errors, unit: events, kind: counter, id: 0x577de621243f5a94 }

            # decode_latency_sum_us: sum of per-frame audio decode latency (output-emitted −
            #                        input-queued). Needs decoder-internal timestamps. Paired
            #                        with frames_decoded.

            # decode_latency_sum_us: sum of per-frame audio decode latency (output-emitted −
            #                        input-queued). Needs decoder-internal timestamps. Paired
            #                        with frames_decoded.

            # decode_latency_sum_us: sum of per-frame audio decode latency (output-emitted −
            #                        input-queued). Needs decoder-internal timestamps. Paired
            #                        with frames_decoded.
            - { name: decode_latency_sum_us, unit: us, kind: counter, id: 0x1b3e9a9b42d47872 }

            # last_decode_error_pts_ms: Stream PTS at detection of the most recent audio decode
            #                           error, within ±1 frame interval. Read with
            #                           decode_errors. Left undeclared where no PTS is derivable
            #                           — never served as -1.

            # last_decode_error_pts_ms: Stream PTS at detection of the most recent audio decode
            #                           error, within ±1 frame interval. Read with
            #                           decode_errors. Left undeclared where no PTS is derivable
            #                           — never served as -1.

            # last_decode_error_pts_ms: Stream PTS at detection of the most recent audio decode
            #                           error, within ±1 frame interval. Read with
            #                           decode_errors. Left undeclared where no PTS is derivable
            #                           — never served as -1.
            - { name: last_decode_error_pts_ms, unit: ms, kind: current, id: 0x5d4a1b8bfa10cbbe }

            # last_decode_error_reason: Closed-vocabulary classification of the most recent
            #                           audio decode error. The value a consumer branches on.

            # last_decode_error_reason: Closed-vocabulary classification of the most recent
            #                           audio decode error. The value a consumer branches on.

            # last_decode_error_reason: Closed-vocabulary classification of the most recent
            #                           audio decode error. The value a consumer branches on.
            - { name: last_decode_error_reason, unit: none, kind: current, id: 0x10596fb80be50427 }

            # last_decode_error_vendor_code: The SoC's own code for the same fault, carried
            #                                through uninterpreted. For vendor diagnosis; never
            #                                branched on.

            # last_decode_error_vendor_code: The SoC's own code for the same fault, carried
            #                                through uninterpreted. For vendor diagnosis; never
            #                                branched on.

            # last_decode_error_vendor_code: The SoC's own code for the same fault, carried
            #                                through uninterpreted. For vendor diagnosis; never
            #                                branched on.
            - { name: last_decode_error_vendor_code, unit: none, kind: current, id: 0x7129e447242c26d5 }

        # Sources    av.audio_sink.0
        # Note       audio presentation, distinct from audio decode - as
        #            av.video_sink is distinct from av.video_decoder
        - element: audio_sink
          instances: 1            # one mixed output path on this product
          fields:

            # underflowed: Count of audio buffer-underflow episodes. +1 at each episode start
            #              (no decoded audio to present); ends when output resumes. Counterpart
            #              of av.video_sink.underflowed — without it an audio underflow rate
            #              cannot be computed at all.

            # underflowed: Count of audio buffer-underflow episodes. +1 at each episode start
            #              (no decoded audio to present); ends when output resumes. Counterpart
            #              of av.video_sink.underflowed — without it an audio underflow rate
            #              cannot be computed at all.

            # underflowed: Count of audio buffer-underflow episodes. +1 at each episode start
            #              (no decoded audio to present); ends when output resumes. Counterpart
            #              of av.video_sink.underflowed — without it an audio underflow rate
            #              cannot be computed at all.
            - { name: underflowed, unit: episodes, kind: counter, id: 0x142f899619c8c4a9 }

            # underflow_duration_ms: Cumulative time the audio path was starved of decoded data
            #                        (sum of of each episode start->clear). Paired with
            #                        underflowed: mean episode length = underflow_duration_ms /
            #                        underflowed.

            # underflow_duration_ms: Cumulative time the audio path was starved of decoded data
            #                        (sum of of each episode start->clear). Paired with
            #                        underflowed: mean episode length = underflow_duration_ms /
            #                        underflowed.

            # underflow_duration_ms: Cumulative time the audio path was starved of decoded data
            #                        (sum of of each episode start->clear). Paired with
            #                        underflowed: mean episode length = underflow_duration_ms /
            #                        underflowed.
            - { name: underflow_duration_ms, unit: ms, kind: counter, id: 0x4ce68288bc2f93b2 }

            # silence_duration_ms: Cumulative emitted digital silence at or above the
            #                      HFP-declared threshold (default 500 ms minimum episode).
            #                      Digital-silence detection is in the audio path. Distinguishes
            #                      unexpected absence of audio output from starvation
            #                      (underflow_duration_ms).

            # silence_duration_ms: Cumulative emitted digital silence at or above the
            #                      HFP-declared threshold (default 500 ms minimum episode).
            #                      Digital-silence detection is in the audio path. Distinguishes
            #                      unexpected absence of audio output from starvation
            #                      (underflow_duration_ms).

            # silence_duration_ms: Cumulative emitted digital silence at or above the
            #                      HFP-declared threshold (default 500 ms minimum episode).
            #                      Digital-silence detection is in the audio path. Distinguishes
            #                      unexpected absence of audio output from starvation
            #                      (underflow_duration_ms).
            - { name: silence_duration_ms, unit: ms, kind: counter, id: 0x09fdcdf6715ef048 }

            # silence_event_count: Count of completed digital-silence periods at or above the
            #                      HFP-declared threshold. Paired with silence_duration_ms: mean
            #                      silence length = silence_duration_ms / silence_event_count.

            # silence_event_count: Count of completed digital-silence periods at or above the
            #                      HFP-declared threshold. Paired with silence_duration_ms: mean
            #                      silence length = silence_duration_ms / silence_event_count.

            # silence_event_count: Count of completed digital-silence periods at or above the
            #                      HFP-declared threshold. Paired with silence_duration_ms: mean
            #                      silence length = silence_duration_ms / silence_event_count.
            - { name: silence_event_count, unit: events, kind: counter, id: 0x357a57be2601412e }

            # buffer_depth_ms: Buffered decoded audio ahead of the play position now. Live
            #                  sample re-read each poll; not cumulative. Warning: SoC- and
            #                  use-case-specific.

            # buffer_depth_ms: Buffered decoded audio ahead of the play position now. Live
            #                  sample re-read each poll; not cumulative. Warning: SoC- and
            #                  use-case-specific.

            # buffer_depth_ms: Buffered decoded audio ahead of the play position now. Live
            #                  sample re-read each poll; not cumulative. Warning: SoC- and
            #                  use-case-specific.
            - { name: buffer_depth_ms, unit: ms, kind: current, id: 0x1aea4fc8a27262b7 }

            # last_underflow_trigger: Closed-vocabulary cause of the most recent audio underflow
            #                         episode, same vocabulary as
            #                         av.video_sink.last_underflow_trigger.

            # last_underflow_trigger: Closed-vocabulary cause of the most recent audio underflow
            #                         episode, same vocabulary as
            #                         av.video_sink.last_underflow_trigger.

            # last_underflow_trigger: Closed-vocabulary cause of the most recent audio underflow
            #                         episode, same vocabulary as
            #                         av.video_sink.last_underflow_trigger.
            - { name: last_underflow_trigger, unit: none, kind: current, id: 0x40771a4a2b2ee087 }

            # last_underflow_duration_ms: Length of the most recent completed audio underflow
            #                             episode. Read with underflowed and
            #                             underflow_duration_ms, which give the count and the
            #                             total.

            # last_underflow_duration_ms: Length of the most recent completed audio underflow
            #                             episode. Read with underflowed and
            #                             underflow_duration_ms, which give the count and the
            #                             total.

            # last_underflow_duration_ms: Length of the most recent completed audio underflow
            #                             episode. Read with underflowed and
            #                             underflow_duration_ms, which give the count and the
            #                             total.
            - { name: last_underflow_duration_ms, unit: ms, kind: current, id: 0x638f410fc657c525 }

            # last_silence_duration_ms: Length of the most recent completed digital-silence
            #                           period. Read with silence_event_count and
            #                           silence_duration_ms.

            # last_silence_duration_ms: Length of the most recent completed digital-silence
            #                           period. Read with silence_event_count and
            #                           silence_duration_ms.

            # last_silence_duration_ms: Length of the most recent completed digital-silence
            #                           period. Read with silence_event_count and
            #                           silence_duration_ms.
            - { name: last_silence_duration_ms, unit: ms, kind: current, id: 0x1cf88c9352312e83 }

        # Sources    av.clock.0
        # Write      getSource("av.clock.0")->setField("sync_threshold_ms", 40)
        #            (the only writable field here; kind: config)
        - element: clock
          instances: 1            # one A/V clock, shared across sessions
          pollCadenceMs: 200      # sync figures move slowly; faster buys nothing
          fields:

            # sync_offset_ms: The driver's predicted current audio-vs-video presentation offset.
            #                 Sign: audio leads -> positive, video leads -> negative. Live
            #                 sample, not cumulative.

            # sync_offset_ms: The driver's predicted current audio-vs-video presentation offset.
            #                 Sign: audio leads -> positive, video leads -> negative. Live
            #                 sample, not cumulative.

            # sync_offset_ms: The driver's predicted current audio-vs-video presentation offset.
            #                 Sign: audio leads -> positive, video leads -> negative. Live
            #                 sample, not cumulative.
            - { name: sync_offset_ms, unit: ms, kind: current, id: 0x0853e5868c5f2bd8 }

            # sync_max_abs_offset_ms: Largest abs(sync_offset_ms) observed since instance
            #                         creation. Monotone non-decreasing high-water mark —
            #                         derived from sync_offset_ms.

            # sync_max_abs_offset_ms: Largest abs(sync_offset_ms) observed since instance
            #                         creation. Monotone non-decreasing high-water mark —
            #                         derived from sync_offset_ms.

            # sync_max_abs_offset_ms: Largest abs(sync_offset_ms) observed since instance
            #                         creation. Monotone non-decreasing high-water mark —
            #                         derived from sync_offset_ms.
            - { name: sync_max_abs_offset_ms, unit: ms, kind: high_water, id: 0x20d08f573aeb1229 }

            # sync_time_over_threshold_ms: Cumulative time abs(sync_offset_ms) exceeded
            #                              sync_threshold_ms — the time integral of out-of-sync
            #                              operation. Derived from sync_offset_ms and
            #                              sync_threshold_ms.

            # sync_time_over_threshold_ms: Cumulative time abs(sync_offset_ms) exceeded
            #                              sync_threshold_ms — the time integral of out-of-sync
            #                              operation. Derived from sync_offset_ms and
            #                              sync_threshold_ms.

            # sync_time_over_threshold_ms: Cumulative time abs(sync_offset_ms) exceeded
            #                              sync_threshold_ms — the time integral of out-of-sync
            #                              operation. Derived from sync_offset_ms and
            #                              sync_threshold_ms.
            - { name: sync_time_over_threshold_ms, unit: ms, kind: counter, id: 0x323c3766c9e3638c }

            # sync_threshold_ms: The offset magnitude beyond which playback counts as out of
            #                    sync. The threshold sync_time_over_threshold_ms integrates
            #                    against and sync_max_abs_offset_ms is judged by — declaring
            #                    those two without this leaves both uninterpretable. Writable so
            #                    an integrator can tighten it per product.

            # sync_threshold_ms: The offset magnitude beyond which playback counts as out of
            #                    sync. The threshold sync_time_over_threshold_ms integrates
            #                    against and sync_max_abs_offset_ms is judged by — declaring
            #                    those two without this leaves both uninterpretable. Writable so
            #                    an integrator can tighten it per product.

            # sync_threshold_ms: The offset magnitude beyond which playback counts as out of
            #                    sync. The threshold sync_time_over_threshold_ms integrates
            #                    against and sync_max_abs_offset_ms is judged by — declaring
            #                    those two without this leaves both uninterpretable. Writable so
            #                    an integrator can tighten it per product.
            - { name: sync_threshold_ms, unit: ms, kind: config, writable: true, id: 0x4c981a6fd5bbf977 }

            # resync_count: Count of A/V-sync corrections the clock applied — each discrete
            #               realignment to re-align audio and video. +1 per applied correction.

            # resync_count: Count of A/V-sync corrections the clock applied — each discrete
            #               realignment to re-align audio and video. +1 per applied correction.

            # resync_count: Count of A/V-sync corrections the clock applied — each discrete
            #               realignment to re-align audio and video. +1 per applied correction.
            - { name: resync_count, unit: events, kind: counter, id: 0x1e11b09c212e1a59 }

            # resync_magnitude_sum_ms: sum of of the absolute magnitude (ms) of each resync
            #                          correction. Clock-internal. Paired with resync_count:
            #                          mean correction size = resync_magnitude_sum_ms /
            #                          resync_count.

            # resync_magnitude_sum_ms: sum of of the absolute magnitude (ms) of each resync
            #                          correction. Clock-internal. Paired with resync_count:
            #                          mean correction size = resync_magnitude_sum_ms /
            #                          resync_count.

            # resync_magnitude_sum_ms: sum of of the absolute magnitude (ms) of each resync
            #                          correction. Clock-internal. Paired with resync_count:
            #                          mean correction size = resync_magnitude_sum_ms /
            #                          resync_count.
            - { name: resync_magnitude_sum_ms, unit: ms, kind: counter, id: 0x7be51ba179aa2379 }

        # Sources    av.session.0, av.session.1
        # Note       served by middleware, not the SoC - the session state
        #            machine is where these figures are observable
        - element: session
          instances: 2            # one per concurrent playback session
          fields:

            # time_playing_ms: Cumulative wall-clock time the session spent in PLAYING. The
            #                  denominator every rate and ratio is computed against — without it
            #                  a consumer cannot turn a drop count into a drop rate. Accumulated
            #                  by the session state machine.

            # time_playing_ms: Cumulative wall-clock time the session spent in PLAYING. The
            #                  denominator every rate and ratio is computed against — without it
            #                  a consumer cannot turn a drop count into a drop rate. Accumulated
            #                  by the session state machine.

            # time_playing_ms: Cumulative wall-clock time the session spent in PLAYING. The
            #                  denominator every rate and ratio is computed against — without it
            #                  a consumer cannot turn a drop count into a drop rate. Accumulated
            #                  by the session state machine.
            - { name: time_playing_ms, unit: ms, kind: counter, id: 0x070f48f400dfd832 }

            # time_buffering_ms: Cumulative time the session was stalled awaiting data, as the
            #                    state machine observed it — distinct from
            #                    av.video_sink.underflow_duration_ms, which is the sink's view
            #                    of starvation.

            # time_buffering_ms: Cumulative time the session was stalled awaiting data, as the
            #                    state machine observed it — distinct from
            #                    av.video_sink.underflow_duration_ms, which is the sink's view
            #                    of starvation.

            # time_buffering_ms: Cumulative time the session was stalled awaiting data, as the
            #                    state machine observed it — distinct from
            #                    av.video_sink.underflow_duration_ms, which is the sink's view
            #                    of starvation.
            - { name: time_buffering_ms, unit: ms, kind: counter, id: 0x23bb1bcb7c5998c5 }

            # preroll_ms: Time from play() to the pipeline reaching PLAYING, per playback start.
            #             With the driver-sourced av.video_sink.first_frame_presented_pts_ms
            #             this splits time-to-first-frame into the part middleware owns and the
            #             part the hardware owns.

            # preroll_ms: Time from play() to the pipeline reaching PLAYING, per playback start.
            #             With the driver-sourced av.video_sink.first_frame_presented_pts_ms
            #             this splits time-to-first-frame into the part middleware owns and the
            #             part the hardware owns.

            # preroll_ms: Time from play() to the pipeline reaching PLAYING, per playback start.
            #             With the driver-sourced av.video_sink.first_frame_presented_pts_ms
            #             this splits time-to-first-frame into the part middleware owns and the
            #             part the hardware owns.
            - { name: preroll_ms, unit: ms, kind: current, id: 0x39fc344ee2e672e7 }

            # seek_count: Completed seek/flush cycles. The denominator for post-seek recovery
            #             figures, and the context that explains why first-frame timing was
            #             re-set.

            # seek_count: Completed seek/flush cycles. The denominator for post-seek recovery
            #             figures, and the context that explains why first-frame timing was
            #             re-set.

            # seek_count: Completed seek/flush cycles. The denominator for post-seek recovery
            #             figures, and the context that explains why first-frame timing was
            #             re-set.
            - { name: seek_count, unit: events, kind: counter, id: 0x1e61cf035b838d2e }

        # Sources    av.admission.0
        # Note       the record of sessions that were refused; a session element
        #            cannot carry these because there is no session to carry them
        - element: admission
          instances: 1            # device-wide admission control
          fields:

            # denied_count: Session-creation requests refused as unsatisfiable. Lets a consumer
            #               distinguish "playback never started" from "playback started and then
            #               failed" — two very different customer experiences, indistinguishable
            #               from playback counters alone.

            # denied_count: Session-creation requests refused as unsatisfiable. Lets a consumer
            #               distinguish "playback never started" from "playback started and then
            #               failed" — two very different customer experiences, indistinguishable
            #               from playback counters alone.

            # denied_count: Session-creation requests refused as unsatisfiable. Lets a consumer
            #               distinguish "playback never started" from "playback started and then
            #               failed" — two very different customer experiences, indistinguishable
            #               from playback counters alone.
            - { name: denied_count, unit: events, kind: counter, id: 0x2f3b167773559817 }

            # reclaim_count: Times a session's resources were reclaimed for another admitted
            #                consumer. Explains an otherwise inexplicable teardown.

            # reclaim_count: Times a session's resources were reclaimed for another admitted
            #                consumer. Explains an otherwise inexplicable teardown.

            # reclaim_count: Times a session's resources were reclaimed for another admitted
            #                consumer. Explains an otherwise inexplicable teardown.
            - { name: reclaim_count, unit: events, kind: counter, id: 0x27a466d1c15111b0 }

    - domain: health
      dictionaryVersion: "1.1"    # revision of the health field dictionary

      elements:

        # Sources    health.poll.0
        # Note       declared per declaring party, so "are these numbers fresh?"
        #            is answerable per source of numbers
        - element: poll
          instances: 1            # one poll loop per declaring party
          fields:

            # poll_period_ms: The poll loop's actual most-recent period, against the declared
            #                 cadence floor. A declared cadence is an intention; this is the
            #                 measurement.

            # poll_period_ms: The poll loop's actual most-recent period, against the declared
            #                 cadence floor. A declared cadence is an intention; this is the
            #                 measurement.

            # poll_period_ms: The poll loop's actual most-recent period, against the declared
            #                 cadence floor. A declared cadence is an intention; this is the
            #                 measurement.
            - { name: poll_period_ms, unit: ms, kind: current, id: 0x6983c999eb4a2057 }

            # poll_overrun_count: Poll cycles that exceeded the declared floor. Direct evidence
            #                     for or against the ≤50 ms freshness contract.

            # poll_overrun_count: Poll cycles that exceeded the declared floor. Direct evidence
            #                     for or against the ≤50 ms freshness contract.

            # poll_overrun_count: Poll cycles that exceeded the declared floor. Direct evidence
            #                     for or against the ≤50 ms freshness contract.
            - { name: poll_overrun_count, unit: events, kind: counter, id: 0x306beef00e98e4dc }

            # state_age_ms: Age of the canonical state at the moment this read was served — how
            #               stale the numbers in this snapshot are. Lets a consumer reason about
            #               freshness instead of assuming it.

            # state_age_ms: Age of the canonical state at the moment this read was served — how
            #               stale the numbers in this snapshot are. Lets a consumer reason about
            #               freshness instead of assuming it.

            # state_age_ms: Age of the canonical state at the moment this read was served — how
            #               stale the numbers in this snapshot are. Lets a consumer reason about
            #               freshness instead of assuming it.
            - { name: state_age_ms, unit: ms, kind: current, id: 0x71a67dc5c92a91bd }
