# The bitbake work tree lives under the repo's build/ dir (build/bitbake), which the
# root .gitignore already covers. Only the staged binder tarball is under this layer:
# prebuilt binder SDK, regenerated by run.sh, never committed.
meta-halif-ci/recipes-binder/linux-binder/files/binder-sdk.tar.gz
